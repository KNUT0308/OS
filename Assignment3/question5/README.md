# Terminal commands for how to run code
1. make
2. /usr/bin/time --verbose ./Question7 128

For switching between initializing and NOT initializing the dynamically allocated memory, comment out the memset command (line 21) or not in hw3_q5_meminfo.c
